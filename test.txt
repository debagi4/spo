int calculate(int a, int b) {
    int res = a + b;
    return res;
}

int main() {
    int x = 10;
    int y = 20;
    int res = calculate(x, y);
    x = x + y;

    if (x > 0) {
        string message = "Hello";
    } else {
        x = calculate(x, y);
        return x;
    }

    while (y > 0) {
        y = y - 1;
    }

    do {
        x = x * 2;
    } while (x < 100);

    bool flag = true;
    char c = 'A';
    int hexVal = 0x1F;
    int bitsVal = 0b1010;

    return res;
}